<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task Details</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
</head>

<body class="bg-light">

    <div class="container mt-5">
        <h1 class="text-center">📋 Task Details</h1>

        <div class="card mt-4">
            <div class="card-body">
                <h3 class="card-title">
                    <%= todo.title %>
                </h3>
                <p><strong>Status:</strong>
                    <%= todo.completed ? "✅ Completed" : "❌ Pending" %>
                </p>
                <p><strong>Created At:</strong>
                    <%= todo.createdAt.toDateString() %>
                </p>

                <div class="d-flex justify-content-between mt-3">
                    <% if (!todo.completed) { %>
                        <a href="/complete/<%= todo._id %>" class="btn btn-success">Done</a>
                        <% } %>
                            <a href="/edit/<%= todo._id %>" class="btn btn-warning">Edit Task</a>
                            <a href="/" class="btn btn-secondary">Back</a>
                            <a href="/delete/<%= todo._id %>" class="btn btn-danger delete-btn">Delete</a>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Custom JS -->
    <script src="/script.js"></script>
</body>

</html>